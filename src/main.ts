// Import the stylesheet for the application
import "./style.css";

// Import the function to set up the input form
import { setupInputForm } from "./inputForm";

// Initialize the input form when the application starts
setupInputForm();
